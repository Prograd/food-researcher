import React from "react";
import { Header } from "../../components/Header";
import "./style.css";

const Delete = () => {
  return(
    <div>
        <Header></Header>
        <h1 className="edit-title">USUŃ LOKAL</h1>
    </div>
  )  
};

export default Delete;